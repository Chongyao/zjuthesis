# 绪论修改检查计划

## 1. 目标
对照审稿人的修改建议，检查 `/home/zcy/workspace/records/zjuthesis/body/graduate/intro/` 目录下的四个 `.tex` 文件，确认各项修改是否均已完成。

## 2. 检查清单

### 2.1 逻辑架构层面
- [ ] **概念引入的“平行性”** (`1_background.tex`)：
  - 检查第三个软硬耦合场景的描述是否已从讨论“基函数”修改为物理/系统层面的表述。
  - 预期修改：“...而在基于模态综合法（CMS）的广义特征值问题中，则体现为**局部高频的强刚性（硬）截断与全局低频的柔顺（软）振动**的严重脱节。”
- [ ] **章节间过渡** (`2_related_and_problems.tex` 到 `3_contributions_and_organization.tex` 的衔接)：
  - 检查第3节（研究问题）的开篇过渡句是否已修改得更加规范和具有承上启下的作用。
  - 预期修改：“基于前文对物理场离散管线及基函数变换理论的探讨，本节将深入剖析物理仿真中三类典型的软硬耦合病态系统。我们将分别揭示其病态刚度的数学来源，并回顾现有主流求解方案及其局限性，从而引出本文的具体研究问题。”
- [ ] **递进关系的提前剧透** (`1_background.tex`)：
  - 检查第1节列出三个应用问题时，是否增加了一句话点出这三个问题在“硬约束抽象程度逐渐增加”上的递进关系。

### 2.2 行文通顺性与学术规范性层面
- [ ] **词语过于绝对或口语化** (`2_related_and_problems.tex` 或对应文件)：
  - 第3.2节（现对应文件名可能为 `2_related_and_problems.tex` 中的子节）：检查是否将“绝对消除”改为“**难以在理论上严格保证消除**”。
  - 第3.3节：检查是否将“近似灾难”改为“**严重的近似截断误差**”或“**基空间拟合困境**”。
  - 第3.3节：检查是否将“凭空变出”改为“**无法从根本上补偿/还原缺失的低频相位信息**”。
- [ ] **术语一致性** (全文)：
  - 检查“弹性棒”、“弹性细杆”、“弹性绳”是否已全文统一（建议为“弹性细杆”或“弹性棒”）。主要涉及 `1_background.tex`、`2_related_and_problems.tex` (子标题) 以及 `3.1_structure.tex` (TikZ图)。
  - 检查 `3.1_structure.tex` 中 TikZ 图右侧第三个柱子的文字是否精简。
  - 预期修改为：“**大规模广义特征值问题（CMS）的频域相位缺失**” 或 “**广义特征值问题中的高频刚性截断**”。
- [ ] **长难句微调**：
  - `1_background.tex`：检查是否在“智能体的学习过程...”之前增加了半句过渡，如“**近年来，随着人工智能浪潮的兴起，物理模拟的研究立意进一步升华。**在具身智能……”。
  - 第3.2节部分（`2_related_and_problems.tex` 中）：检查是否将“几何可视化上的瑕疵”修改为更严谨的表述，如“**网格质量的极端劣化不仅是表面几何表征的瑕疵，更严重的是……**”。

## 3. 检查方式
使用执行者（Executor）代理，通过运行 `grep` 或 `cat` 命令，查看上述文件的具体内容，并与检查清单逐一核对。

## 4. 输出
最终给出检查报告，明确列出哪些已完成，哪些未完成（如有），并提供未完成项的修复建议。