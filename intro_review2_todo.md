# 绪论修改待办清单 (intro_review2_todo.md)

以下是根据审稿人意见整理的修改待办清单，按照优先级由高到低排列。您可以直接在“完成状态”列打勾（修改 `[ ]` 为 `[x]`）来追踪进度。

## 修改任务总览

| 优先级 | 维度 | 具体问题描述 | 修改思路 / 建议 | 完成状态 |
| :--- | :--- | :--- | :--- | :---: |
| **第一优先级** | **格式规范** | **引用格式不统一**。开头使用了手写编号 `[1]`，后文使用了 `\cite{}`。 | 统一将全文所有手写编号替换为标准的 `~\cite{...}`。 | [x] |
| **第一优先级** | **格式规范** | **存在示意占位图**。正文中遗留了 `example-image` 等占位符。 | 替换占位图为真实的、带有具体意义的配图。如果有图还没画完，**待定**。 | [ ] |
| **第一优先级** | **格式规范** | **最后一张总览图不规范**。TikZ 图裸露在正文中，缺少浮动体环境。 | 为 TikZ 图包上 `\begin{figure}[htbp] ... \end{figure}`，并补充 `\caption` 和 `\label`，在正文中增加交叉引用。 | [x] |
| **第一优先级** | **格式细节** | **存在 Typo 和术语不统一**。“不可拉伸”与“不可伸长”混用；公式存在拼写错误（如 `sitff`）。 | 1. 全文检索 `sitff` 改为 `stiff`。<br>2. 统一将细杆相关的描述定为“不可伸长”（或根据第二章统一）。<br>3. 小节标题冒号后的多余空格删掉。 | [x] |
| **第二优先级** | **逻辑主线** | **“基础计算管线”部分篇幅过重**，包含了过多数学推导，像理论预备章，拖慢了引出问题的节奏。 | **不做**。保留现有理论推导铺垫。 | [x] |
| **第二优先级** | **内容叙述** | **判断过于绝对、立场宣示过多**（如“唯有”、“彻底”、“全新范式”）。 | 减少情绪化词汇30%，改为限定性论述（如“对于本文关注的极端病态场景……”、“本文尝试提出统一视角”）。 | [x] |
| **第二优先级** | **逻辑主线** | **三个问题强行统一显得生硬**，尤其是第三个（相位缺失）与前两个的联系不够直观。 | **放弃**。由于统一表格容易带来别扭感，不再使用表格强行概括，维持原有文字的表述框架。 | [x] |
| **第三优先级** | **内容补充** | **缺少明确的“研究问题 (Research Questions)”界定**。 | 单列一段，明确提出 3 个具体的 RQ。 | [x] |
| **第三优先级** | **内容补充** | **缺少方法论的“适用范围与边界”声明**。 | 补充一段声明，明确本文方法并非包治百病，而是聚焦于特定的典型软硬耦合结构。 | [x] |
| **第三优先级** | **格式细节** | **图中文字语言风格不统一**。部分图中含有纯英文标签（如 `degenerate cell`, `Boundary lock`）。 | 将图中的英文标签修改为全中文，或采用“中文 (英文)”的双语对照格式。 | [ ] |
| **第三优先级** | **格式命名** | **文件命名与实际内容不完全匹配**（如 `2_related_and_problems.tex` 包含了大量管线基础知识）。 | **待定**。可考虑重命名文件和章节 `\section` 标题以更准确反映“基础管线与问题引出”的本质。 | [ ] |
| **第三优先级** | **格式细节** | **数学记号和公式表达不够严谨统**一。如 `\mathbf{x}` 与 `\mathbf{q}` 的衔接；部分公式的叉乘符号用法不明确；索引记号 `ii` 与 `\mathcal{I}\mathcal{I}` 混用。 | 已将绪论中的状态空间符号体系统一化：<br>1. 统一使用 $\mathbf{q}$ 代表离散广义自由度。<br>2. 广义特征值问题中原本混用的 $\mathbf{x}$ 改为了 $\mathbf{u}$ 或 $\mathbf{q}$ 以衔接上下文。 | [x] |
| **持续优化** | **叙述凝练** | **段落信息密度不均，长句过多**，单段落承载了定义、抽象、评价等多个目标。 | 在后续修改中贯彻“一段一意”原则（现象段 $\rightarrow$ 瓶颈段 $\rightarrow$ 切入点段），拆解长难句。 | [ ] |